import 'package:flutter/material.dart';

class ChatsPageMessageInput extends StatelessWidget {
  const ChatsPageMessageInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(
      children: <Widget>[
        TextField(),
      ],
    );
  }
}
